# clang-tidy >= 17 expected.
#
# Conservative starter set: catches obvious bugs, leaves stylistic checks
# off. As the codebase modernizes (Phase 1+), additional check groups will
# be enabled and the noisy ones suppressed via NOLINT comments at the
# call site, not via a global -* exclusion.
#
# Currently CI runs clang-tidy as warnings-only (does not fail the build).
# In Phase 2 it becomes an error gate after the major refactors land.

---
Checks: >
  bugprone-*,
  clang-analyzer-*,
  cppcoreguidelines-init-variables,
  cppcoreguidelines-narrowing-conversions,
  cppcoreguidelines-pro-type-cstyle-cast,
  cppcoreguidelines-pro-type-member-init,
  cppcoreguidelines-slicing,
  modernize-deprecated-headers,
  modernize-loop-convert,
  modernize-make-shared,
  modernize-make-unique,
  modernize-redundant-void-arg,
  modernize-use-emplace,
  modernize-use-equals-default,
  modernize-use-equals-delete,
  modernize-use-nullptr,
  modernize-use-override,
  modernize-use-using,
  performance-*,
  portability-*,
  readability-avoid-const-params-in-decls,
  readability-braces-around-statements,
  readability-const-return-type,
  readability-container-size-empty,
  readability-delete-null-pointer,
  readability-deleted-default,
  readability-duplicate-include,
  readability-isolate-declaration,
  readability-make-member-function-const,
  readability-misleading-indentation,
  readability-misplaced-array-index,
  readability-non-const-parameter,
  readability-redundant-control-flow,
  readability-redundant-declaration,
  readability-redundant-string-cstr,
  readability-redundant-string-init,
  readability-simplify-boolean-expr,
  readability-static-accessed-through-instance,
  readability-string-compare,
  readability-uniqueptr-delete-release,
  -bugprone-easily-swappable-parameters,
  -bugprone-narrowing-conversions,
  -bugprone-implicit-widening-of-multiplication-result,
  -bugprone-branch-clone,
  -clang-analyzer-cplusplus.NewDeleteLeaks,
  -performance-no-int-to-ptr,
  -performance-avoid-endl

# Warnings-only mode for now. Flip to '*' once the codebase is clean.
WarningsAsErrors: ''

# Don't lint third-party headers we don't control.
HeaderFilterRegex: '^.*/(include|src|tests)/(?!.*third_party).*\.(h|hpp|hh|hxx|cuh|tpp|inl|ipp)$'

FormatStyle: file

CheckOptions:
  - key: readability-braces-around-statements.ShortStatementLines
    value: '2'
  - key: readability-identifier-naming.ClassCase
    value: 'CamelCase'
  - key: readability-identifier-naming.NamespaceCase
    value: 'lower_case'
  - key: bugprone-argument-comment.StrictMode
    value: 'true'
  - key: cppcoreguidelines-init-variables.MathHeader
    value: '<cmath>'
  - key: modernize-use-override.OverrideSpelling
    value: 'override'
  - key: modernize-use-override.FinalSpelling
    value: 'final'
  - key: performance-for-range-copy.WarnOnAllAutoCopies
    value: 'true'
  - key: performance-unnecessary-value-param.AllowedTypes
    value: ''
